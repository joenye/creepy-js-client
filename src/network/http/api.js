// import { getJson } from './client.js';

// const login = (body) => postJson('/login', {raw: true, body})
//   .then(result => result.json().then(user => ({
//     user: user,
//     api_token: result.headers.get('X-Api-Token')
//   })))

// export const loginWithEmailPassword = ({email, password}) => login({ email, password })
